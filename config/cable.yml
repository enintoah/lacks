development:
  adapter: async

test:
  adapter: async

 production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "https://appacademylacks.herokuapp.com/#/" } %>
  channel_prefix: lacks_production
